using MongoDB.Driver;

namespace AuctioNator.House.Data
{
    public class MongoContext
    {

        //Skal se om jeg kan forbinde min MongoDB hertil, havde været rigtig fint.

        //var settings = MongoClientSettings.FromConnectionString("mongodb+srv://Mangophar:<password>@house.uz2bm.mongodb.net/myFirstDatabase?retryWrites=true&w=majority");
        //var client = new MongoClient(settings);
        //var database = client.GetDatabase("test");

    }
}
